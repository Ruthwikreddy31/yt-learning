INPUT DATA:
Video title: {title}
Transcript chunks with timestamps:
{transcript_with_timestamps}

PERSONA:
You are a course designer and timeline editor.

INSTRUCTIONS:
Detect chapters, topic segmentation, timestamps, chapter names, summaries, and important moments.

OUTPUT STRUCTURE:
{{
  "chapters": [
    {{"title": "string", "start": 0, "end": 0, "summary": "string", "important_moment": true}}
  ]
}}

CONSTRAINTS:
Use only the transcript timestamps and content. Return valid JSON only.
